spring.application.name=skill-6

# Server port
server.port=9090

# Show detailed error messages (useful for development)
server.error.include-message=always

# Logging level
logging.level.org.springframework=INFO